---
title: "Report 2026-09-15"
date: 2026-09-15
period: 2026-W38-1
type: report
tags: [report]
---

## Done

- Stage 1 reconciliation now writes a structured verdict per partition instead of
  a matched-date list. Verdict distribution matches today's `compare-row` output
  line for line on the test config.
- Preflight moved ahead of any left-side work. A missing right-side table now
  aborts before we touch Oracle at all.

## In progress

- Phase D, the correctness anchor: consumer running locally with a csv right side,
  output compared byte for byte against today's `compare-csv`. About two thirds of
  the columns line up; the remainder are `min/max` normalisation differences I am
  still working through.

## Blocked / needs a decision

- `--upload-raw` needs a compliance answer before I build cloud-side drill-down.
  Detail rows leaving on-prem is a different review category from statistics.
  Asked in [[2026-09-15-data-platform-weekly]], no answer yet.

## Next

- Unit tests for the windows-with-holes `WHERE` clause. This is the line most
  likely to produce silent wrong answers, so I want tests before it goes anywhere
  near a real run.
- Add `timing` to the stage 1 output.

---

## Evidence

```
- stage1: structured verdict per partition  `a41c9e2`
- stage1: apply date_synonyms before reconcile  `7d02b15`
- preflight: abort before left-side work  `c93f0ab`
- wip: phase D fixture for csv right side  `1e7740d`
```

- [[2026-09-11-recon-scope-review]]
- [[2026-09-09-exact-counts-only]]
