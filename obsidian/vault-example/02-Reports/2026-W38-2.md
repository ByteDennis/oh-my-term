---
title: "Report 2026-09-18"
date: 2026-09-18
period: 2026-W38-2
type: report
tags: [report]
---

## Done

- Phase D passes. Local consumer output is byte-identical to `compare-csv` for the
  full test config. This was the gate before touching real Databricks, so that
  work can now start.
- Unit tests for windows with holes: hole in the middle, holes at both ends, and a
  window reduced to a single day. Found a real bug in the third case, where an
  empty interval list produced a `WHERE` clause matching everything.

## In progress

- `timing` fields in stage 1 output, most of the way there.

## Blocked / needs a decision

- Still waiting on the `--upload-raw` compliance answer.
- Oracle session budget. We are allowed 2, which caps stage 1 concurrency for the
  two largest pairs. Asked the DBA team on Tuesday.

## Next

- Start phase E: real Databricks as the right side.
- Measure `idle_ratio` across a full five-pair run to pick a worker count.

---

## Evidence

```
- tests: window WHERE with holes (3 shapes)  `b8e1d4c`
- fix: empty interval list matched all partitions  `55af029`
- phase D: byte-identical output achieved  `0c7fe93`
- wip: stage1 timing fields  `9a3b6e1`
```

The single-day-window bug is worth noting: it would not have failed loudly. The
comparison would simply have been run over partitions stage 1 had excluded, and
the results would have looked like data problems rather than a code problem.
